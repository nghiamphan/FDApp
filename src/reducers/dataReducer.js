const initialState = {
	titles: [],
	posts: [],
	comments: []
}
const dataReducer = (state = initialState, action) => {
	switch (action.type) {
	default:
		return state
	}
}

export default dataReducer